<h1>Currency pairs</h1>

<%#= @parsed_data.keys%>
<ul>
  <% @currencies.keys.each do |symbol| %>
    <li>
      <a href="/<%= symbol %>">Convert 1 <%= symbol %> to ...</a>
    </li>

  <% end %>
</ul>
